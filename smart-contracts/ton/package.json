{
  "name": "oxmart-ton-contracts",
  "version": "1.0.0",
  "description": "0xMart Payment Contract for TON Blockchain",
  "scripts": {
    "build": "tact --config tact.config.json",
    "deploy:testnet": "ts-node scripts/deploy.ts --network=testnet",
    "deploy:mainnet": "ts-node scripts/deploy.ts --network=mainnet",
    "add-tokens:testnet": "ts-node scripts/add-tokens.ts --network=testnet",
    "add-tokens:mainnet": "ts-node scripts/add-tokens.ts --network=mainnet"
  },
  "dependencies": {
    "@ton/core": "^0.56.0",
    "@ton/crypto": "^3.2.0",
    "@ton/ton": "^13.11.0"
  },
  "devDependencies": {
    "@tact-lang/compiler": "^1.4.3",
    "@types/node": "^20.11.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
